import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'OpenMAIC Main',
  description: 'Preserved route for the main OpenMAIC program page.',
};

export { default } from '../page';
